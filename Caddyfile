{
	auto_https off
	http_port 2800
}

http://s3browser.localhost {
	bind 127.0.0.1 ::1
	reverse_proxy localhost:{$S3BROWSER_PORT}
}

http://minios3.localhost, http://*.minios3.localhost {
	bind 127.0.0.1 ::1
	reverse_proxy localhost:{$MINIO_PORT}
}

http://minioconsole.localhost {
	bind 127.0.0.1 ::1
	reverse_proxy localhost:{$MINIO_CONSOLE_PORT}
}

# http://weeds3.localhost, http://*.weeds3.localhost {
# 	bind 127.0.0.1 ::1
# 	reverse_proxy localhost:{$WEED_S3_PORT}
# }

http:// {
	bind 127.0.0.1 ::1
	respond "Not Found caddy" 404
}
